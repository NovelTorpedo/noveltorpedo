/**
 * Bootstrap in our global/window dependencies.
 */
window._ = require('lodash');
window.$ = window.jQuery = require('jquery');

/**
 * Bootstrap non-global dependencies.
 */
require('bootstrap-sass');
